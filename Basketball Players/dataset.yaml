# Ultralytics resolves train/val relative to this YAML's directory when `path` is omitted.
# Do NOT use `path: .` — Ultralytics treats that as the process cwd, not this folder.
train: train/images
val: valid/images
test: test/images

nc: 9
names: ['Ball', 'Hoop', 'Period', 'Player', 'Ref', 'Shot Clock', 'Team Name', 'Team Points', 'Time Remaining']
