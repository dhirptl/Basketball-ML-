# Paths are relative to this YAML's directory when `path` is omitted (Ultralytics).
# Do NOT use `path: .` — it resolves to the process cwd, not this folder.
train: train/images
val: valid/images

kpt_shape: [8, 3]
flip_idx: []

nc: 1
names: ["Court"]
